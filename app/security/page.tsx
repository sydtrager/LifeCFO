import {LegalPage} from "@/components/LegalPage";
export default function Page(){return <LegalPage title="Security"><h2>Household isolation</h2><p>Every application table uses Supabase Row Level Security. Explicit select, insert, update, and delete policies scope access through authenticated household membership. Browser code never receives a service-role secret.</p><h2>Documents</h2><p>PDF and CSV statements are limited to 10 MB, stored in a private bucket, and addressed under a household-scoped path. Extracted data is a preview until the user confirms it.</p><h2>Defense in depth</h2><p>LifeCFO applies server validation, rate-limit interfaces, structured redacted logging, audit events, secure error handling, private storage, masked identifiers, and least-privilege credentials. Operators should enable MFA for Supabase and Vercel accounts.</p></LegalPage>}
